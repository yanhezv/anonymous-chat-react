import React from "react";

// import styles from "./Chat.styled";

interface ChatProps {
}

export const Chat: React.FC<ChatProps> = ({ ...props}) => {

   return (
      <section>
         <span>Chat</span>
      </section>
   );
}
